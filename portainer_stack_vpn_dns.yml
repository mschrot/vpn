# Erstellt von Michael Schrot
# YouTube.com/@MSCHROT
# -------------------------------

version: "3.9"

services:
  adguardhome:
    image: adguard/adguardhome:latest
    container_name: adguardhome
    restart: unless-stopped

    environment:
      - TZ=Europe/Berlin

    ports:
      - "53:53/tcp"
      - "53:53/udp"
      - "81:80/tcp" # ⚠️ Admin-Panel!
      - "3000:3000/tcp" # ⚠️ Nach Setup schließe!

    volumes:
      - adguard-work:/opt/adguardhome/work
      - adguard-conf:/opt/adguardhome/conf

    networks:
      vpn_net:
        ipv4_address: 10.10.0.2

  wireguard:
    image: weejewel/wg-easy
    container_name: wireguard
    restart: unless-stopped

    environment:
      - WG_HOST=203.0.113.99 # ⚠️ IP ändern!
      - PASSWORD=mein_passwort # ⚠️ passwort ändern!
      - WG_DEFAULT_DNS=10.10.0.2
      - WG_PORT=51820

    ports:
      - "51820:51820/udp"
      - "51821:51821/tcp" # ⚠️ Admin-Panel!

    volumes:
      - wireguard-data:/etc/wireguard

    cap_add:
      - NET_ADMIN
      - SYS_MODULE

    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1

    networks:
      vpn_net:
        ipv4_address: 10.10.0.3

networks:
  vpn_net:
    driver: bridge
    ipam:
      config:
        - subnet: 10.10.0.0/24

volumes:
  adguard-work:
  adguard-conf:
  wireguard-data:
