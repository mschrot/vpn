# Erstellt von Michael Schrot
# YouTube.com/@MSCHROT
# -------------------------------

version: "3.8"

services:
  wg-easy:
    image: weejewel/wg-easy
    container_name: wg-easy
    environment:
      - WG_HOST=192.168.0.10   # IP-Server ändern! 
      - PASSWORD=de!npa$$w0rd_ # unbedingt ändern!
      - WG_DEFAULT_DNS=1.1.1.1
      - WG_PORT=51820
    volumes:
      - ./config:/etc/wireguard
    ports:
      - "51820:51820/udp"
      - "51821:51821/tcp"  # Web GUI
    restart: unless-stopped
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv4.conf.all.src_valid_mark=1